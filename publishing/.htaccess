# only builder.php (the compilation entry point) should be reachable here
<FilesMatch "^(?!builder\.php$).+$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
